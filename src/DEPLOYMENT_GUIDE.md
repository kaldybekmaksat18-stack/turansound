# 🚀 Инструкция по размещению TuranSound на бесплатном хостинге

## 📋 Содержание
1. [Подготовка проекта](#подготовка-проекта)
2. [Деплой на Vercel (Рекомендуется)](#деплой-на-vercel)
3. [Деплой на Netlify (Альтернатива)](#деплой-на-netlify)
4. [Настройка переменных окружения](#настройка-переменных-окружения)
5. [Проверка работоспособности](#проверка-работоспособности)
6. [Устранение проблем](#устранение-проблем)

---

## 🎯 Подготовка проекта

### 1. Установите Git (если еще не установлен)
```bash
# Windows: скачайте с https://git-scm.com/download/win
# Mac: 
brew install git
# Linux:
sudo apt-get install git
```

### 2. Создайте Git репозиторий

#### Вариант А: Через GitHub Desktop (проще)
1. Скачайте GitHub Desktop: https://desktop.github.com/
2. Откройте GitHub Desktop
3. File → Add Local Repository → выберите папку с проектом
4. Нажмите "Create a Repository" если репозиторий еще не создан
5. Publish repository → выберите имя (например, "turansound")
6. Опубликуйте как публичный или приватный репозиторий

#### Вариант Б: Через командную строку
```bash
# Перейдите в папку проекта
cd путь/к/вашему/проекту

# Инициализируйте Git репозиторий
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit - TuranSound platform"

# Создайте репозиторий на GitHub (https://github.com/new)
# Затем подключите его:
git remote add origin https://github.com/ваш-username/turansound.git
git branch -M main
git push -u origin main
```

---

## 🟣 Деплой на Vercel (Рекомендуется)

### Почему Vercel?
- ✅ **Бесплатный план**: идеален для React приложений
- ✅ **Автоматический деплой**: при каждом push в GitHub
- ✅ **Отличная производительность**: CDN по всему миру
- ✅ **Простая настройка**: 5 минут до запуска
- ✅ **SSL сертификат**: бесплатный HTTPS

### Шаги деплоя:

#### 1. Создайте аккаунт на Vercel
1. Перейдите на https://vercel.com
2. Нажмите "Sign Up"
3. Войдите через GitHub (рекомендуется)

#### 2. Импортируйте проект
1. После входа нажмите "Add New..." → "Project"
2. Выберите репозиторий "turansound" из списка
3. Если не видите репозиторий, нажмите "Adjust GitHub App Permissions"

#### 3. Настройте проект
```
Framework Preset: Vite
Build Command: npm run build
Output Directory: dist
Install Command: npm install
```

Эти настройки должны определиться автоматически благодаря `vercel.json`.

#### 4. Добавьте переменные окружения
**ВАЖНО!** Перед деплоем добавьте переменные:

Нажмите "Environment Variables" и добавьте:

| Name | Value |
|------|-------|
| `VITE_SUPABASE_PROJECT_ID` | `hpcwkbkglggimwcbxpoh` |
| `VITE_SUPABASE_ANON_KEY` | (ваш ключ из Supabase) |

**Где найти ключи:**
1. Откройте https://app.supabase.com
2. Выберите ваш проект
3. Settings → API
4. Скопируйте "Project URL" (последняя часть это ID)
5. Скопируйте "anon public" ключ

#### 5. Деп��ой!
1. Нажмите "Deploy"
2. Подождите 2-3 минуты
3. Готово! Получите ссылку типа `https://turansound.vercel.app`

#### 6. Настройте кастомный домен (опционально)
1. В Vercel → Settings → Domains
2. Добавьте свой домен (можно купить или использовать бесплатный от Vercel)

---

## 🔵 Деплой на Netlify (Альтернатива)

### Почему Netlify?
- ✅ Бесплатный план с щедрыми лимитами
- ✅ Простой интерфейс
- ✅ Хорошая документация

### Шаги деплоя:

#### 1. Создайте аккаунт на Netlify
1. Перейдите на https://netlify.com
2. Нажмите "Sign Up"
3. Войдите через GitHub

#### 2. Создайте новый сайт
1. Нажмите "Add new site" → "Import an existing project"
2. Выберите "GitHub"
3. Выберите репозиторий "turansound"

#### 3. Настройте сборку
```
Build command: npm run build
Publish directory: dist
```

Эти настройки читаются из `netlify.toml`.

#### 4. Добавьте переменные окружения
1. Site settings → Environment variables
2. Добавьте те же переменные что и для Vercel:
   - `VITE_SUPABASE_PROJECT_ID`
   - `VITE_SUPABASE_ANON_KEY`

#### 5. Деплой!
1. Нажмите "Deploy site"
2. Получите ссылку типа `https://turansound.netlify.app`

---

## 🔧 Настройка переменных окружения

### Важность переменных окружения
Переменные окружения позволяют:
- 🔒 Скрыть чувствительные данные из кода
- 🔄 Использовать разные настройки для разработки и продакшена
- 🛡️ Защитить API ключи

### Получение ключей Supabase

#### Шаг 1: Откройте Supabase Dashboard
1. Перейдите на https://app.supabase.com
2. Войдите в аккаунт
3. Выберите ваш проект "TuranSound" (или как вы его назвали)

#### Шаг 2: Найдите настройки API
1. В боковом меню нажмите **Settings** (⚙️)
2. Выберите **API**
3. Прокрутите до секции "Project API keys"

#### Шаг 3: Скопируйте ключи
Вы увидите:
- **Project URL**: `https://hpcwkbkglggimwcbxpoh.supabase.co`
  - Project ID это часть после `https://` и до `.supabase.co`
  - В данном случае: `hpcwkbkglggimwcbxpoh`

- **anon public**: начинается с `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
  - Это публичный ключ, безопасен для использования в frontend

⚠️ **ВАЖНО**: Никогда не используйте `service_role` ключ в frontend!

### Установка переменных в Vercel
1. Vercel Dashboard → ваш проект
2. Settings → Environment Variables
3. Добавьте каждую переменную:
   ```
   Name: VITE_SUPABASE_PROJECT_ID
   Value: hpcwkbkglggimwcbxpoh
   Environment: Production, Preview, Development
   ```
   ```
   Name: VITE_SUPABASE_ANON_KEY
   Value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   Environment: Production, Preview, Development
   ```

### Установка переменных в Netlify
1. Netlify Dashboard → ваш сайт
2. Site settings → Environment variables
3. Нажмите "Add a variable"
4. Добавьте обе переменные

---

## ✅ Проверка работоспособности

### Чек-лист после деплоя:
- [ ] Сайт открывается по ссылке
- [ ] Главная страница загружается
- [ ] Можно перейти в каталог
- [ ] Видны карточки артистов
- [ ] Работает регистрация
- [ ] Работает вход в систему
- [ ] Можно создать бронирование
- [ ] AI-ассистент отвечает

### Проверка переменных окружения
Откройте консоль браузера (F12) и введите:
```javascript
console.log(import.meta.env.VITE_SUPABASE_PROJECT_ID)
```
Должен вывести ваш Project ID.

### Проверка Supabase подключения
В консоли браузера:
```javascript
// Должно быть определено
console.log(window.supabase)
```

---

## 🔥 Устранение проблем

### Проблема: Нет дизайна/стилей на сайте (белый экран с текстом)

**Симптомы:**
- HTML загружается ✅
- Текст виден ✅
- Стили не применяются ❌
- Нет градиентов, цветов, отступов

**Причина:** Не настроен Tailwind CSS v4

**Решение:** ✅ **УЖЕ ИСПРАВЛЕНО!**

Проект использует Tailwind CSS v4 с новым синтаксом. Все необходимые файлы созданы:
- ✅ `/styles/globals.css` - содержит `@import "tailwindcss"`
- ✅ `/tailwind.config.js` - конфигурация Tailwind
- ✅ `/postcss.config.js` - PostCSS плагины
- ✅ `/vite.config.ts` - настройка CSS обработки

**Если проблема всё равно есть:**
```bash
# 1. Удалите кэш
rm -rf node_modules/.vite dist

# 2. Пересоберите
npm install
npm run build

# 3. Проверьте локально
npm run preview

# 4. Если работает локально - закоммитьте и задеплойте заново
git add .
git commit -m "Fix CSS"
git push
```

📖 **Подробности:** См. [CSS_FIX.md](./CSS_FIX.md)

### Проблема: "Build failed" при деплое

**Решение:**
```bash
# Проверьте локально:
npm install
npm run build

# Если есть ошибки TypeScript:
npm run build -- --no-typecheck
```

Обновите `package.json`:
```json
"scripts": {
  "build": "vite build"
}
```

### Проблема: Белый экран после деплоя

**Причина:** Не настроены переменные окружения

**Решение:**
1. Проверьте, что добавили обе переменные в Vercel/Netlify
2. Пересоберите проект (Vercel → Deployments → три точки → Redeploy)

### Проблема: Ошибка "Failed to fetch"

**Причина:** Неправильный Supabase URL или ключ

**Решение:**
1. Проверьте переменные окружения
2. Убедитесь, что используете `anon public` ключ, а не `service_role`
3. Проверьте, что Project ID совпадает

### Проблема: База данных не работает

**Причина:** Не выполнены миграции в Supabase

**Решение:**
1. Откройте Supabase Dashboard
2. SQL Editor
3. Выполните миграции из `/supabase/migrations/`:
   - `000_COMPLETE_SETUP.sql`
   - `001_create_profiles.sql`
   - `002_artists_catalog_and_bookings.sql`
   - `003_fix_rls_policies.sql`

### Проблема: "404 Not Found" на странице каталога

**Причина:** Не настроен routing для SPA

**Решение:**
- Для Vercel: уже настроено в `vercel.json`
- Для Netlify: уже настроено в `netlify.toml`
- Проверьте, что файлы существуют

### Проблема: Медле��ная загрузка

**Решение:**
1. Включите кэширование в Vercel/Netlify (уже настроено)
2. Оптимизируйте изображения
3. Проверьте размер bundle:
   ```bash
   npm run build
   # Смотрите размеры файлов в dist/
   ```

---

## 📊 Мониторинг и аналитика

### Vercel Analytics (бесплатно)
1. Vercel Dashboard → ваш проект
2. Analytics → Enable
3. Смотрите статистику посещений

### Supabase Logs
1. Supabase Dashboard → Logs
2. Смотрите запросы к БД и ошибки

---

## 🔄 Автоматический деплой

### Настройка CI/CD (уже работает!)
После первого деплоя:
1. Делаете изменения в коде
2. Коммитите в Git:
   ```bash
   git add .
   git commit -m "Добавил новую функцию"
   git push
   ```
3. Vercel/Netlify автоматически:
   - Обнаруживает новый коммит
   - Собирает проект
   - Деплоит на production
   - Отправляет уведомление

---

## 🎉 Готово!

Ваш проект TuranSound теперь доступен онлайн!

### Следующие шаги:
- 📱 Поделитесь ссылкой с тестировщиками
- 🔍 Настройте Google Analytics (опционально)
- 📧 Настройте уведомления о деплое
- 🌐 Купите домен (например, turansound.kz)
- 📈 Мониторьте производительность

### Полезные ссылки:
- 📖 Vercel Docs: https://vercel.com/docs
- 📖 Netlify Docs: https://docs.netlify.com
- 📖 Supabase Docs: https://supabase.com/docs
- 💬 Техподдержка: support@vercel.com

---

## 📞 Нужна помощь?

Если возникли проблемы:
1. Проверьте раздел "Устранение проблем" выше
2. Посмотрите логи в Vercel/Netlify Dashboard
3. Проверьте Supabase Logs
4. Откройте консоль браузера (F12) для ошибок

**Удачи с запуском! 🚀**