# 🚀 Полное руководство по установке TuranSound

## 📋 Быстрый старт

Вся система TuranSound теперь полностью интегрирована с Supabase и готова к работе!

### Что реализовано:

✅ **A) Синхронизация артистов с базой данных**
- 180 реальных казахстанских артистов
- Автоматическая загрузка в Supabase
- Редактирование профилей через интерфейс

✅ **B) Система реальных бронирований**
- Создание и управление бронированиями
- Статусы: pending → confirmed → in_progress → completed
- Эскроу-счета и контракты

✅ **C) Realtime чат между заказчиками и артистами**
- Мгновенная доставка сообщений
- Уведомления о непрочитанных
- Привязка к бронированиям

✅ **D) Система рейтингов и отзывов**
- Детальные оценки по 5 критериям
- Ответы артистов на отзывы
- Автоматический пересчёт рейтинга

---

## 🎯 Шаг 1: Выполните миграцию базы данных

### 1.1. Откройте Supabase Dashboard

```
https://supabase.com/dashboard/project/YOUR_PROJECT_ID/editor
```

### 1.2. Перейдите в SQL Editor

Нажмите **SQL Editor** в левом меню → **New Query**

### 1.3. Выполните миграцию #2

Скопируйте содержимое файла:
```
/supabase/migrations/002_artists_catalog_and_bookings.sql
```

И выполните его в SQL Editor (кнопка **Run** или `Ctrl+Enter`)

Эта миграция создаст:
- ✅ Таблицу `artists_catalog` (каталог артистов)
- ✅ Таблицу `bookings` (бронирования)
- ✅ Таблицу `chat_messages` (чат)
- ✅ Таблицу `reviews` (отзывы)
- ✅ Триггеры для автоматического обновления
- ✅ Row Level Security (RLS) политики
- ✅ Индексы для быстрого поиска

---

## 🎯 Шаг 2: Загрузите артистов в базу данных

### 2.1. Откройте админ-панель

В footer страницы нажмите **🔧 Админ-панель (Dev)** или перейдите по URL:
```
/?page=admin-seed
```

### 2.2. Загрузите артистов

1. Нажмите кнопку **"Обновить"** для проверки статуса базы
2. Нажмите **"Загрузить артистов"**
3. Подтвердите действие
4. Дождитесь завершения (около 30 секунд)
5. Готово! ✅ 180 артистов загружены в базу

---

## 🎯 Шаг 3: Проверьте работу системы

### 3.1. Каталог артистов

Перейдите в **Каталог** → артисты теперь загружаются из Supabase в реальном времени.

### 3.2. Бронирование

1. Выберите любого артиста
2. Нажмите "Забронировать"
3. Заполните форму
4. Бронирование сохраняется в базу данных!

### 3.3. Чат

После создания бронирования:
1. Откройте страницу бронирования
2. Нажмите кнопку чата (MessageCircle)
3. Отправьте сообщение артисту
4. Realtime доставка работает!

### 3.4. Отзывы

После завершения мероприятия:
1. Перейдите в профиль артиста
2. Оставьте отзыв с детальными оценками
3. Рейтинг артиста обновится автоматически

---

## 📊 Структура базы данных

### Таблицы:

#### `artists_catalog` (Каталог артистов)
- Трехслойная архитектура (section → roles → genres)
- Портфолио (bio, videos, photos, audio)
- Финансы (price_from, price_to, currency)
- Репутация (rating, review_count, booking_count)

#### `bookings` (Бронирования)
- Детали мероприятия (date, time, location, guest_count)
- Статусы (pending, confirmed, in_progress, completed, cancelled, disputed)
- Финансы (total_price, payment_status, escrow_amount)
- Контракты (contract_signed, contract_url)

#### `chat_messages` (Чат)
- Realtime сообщения
- Статусы прочтения
- Типы (text, image, file, audio, system)

#### `reviews` (Отзывы)
- Общий рейтинг (1-5 звёзд)
- Детальные оценки (профессионализм, общение, качество, цена)
- Текстовый отзыв (плюсы, минусы)
- Ответы артистов

---

## 🔧 API и хуки

### Хуки для работы с данными:

#### `useArtistsCatalog(filters)` - Каталог артистов
```typescript
const { artists, loading, count, refetch } = useArtistsCatalog({
  section: 'stage_artists',
  genres: ['jazz', 'pop'],
  city: 'Алматы',
  minPrice: 100000,
  maxPrice: 500000
});
```

#### `useBookings(userId)` - Бронирования
```typescript
const { bookings, createBooking, updateBookingStatus } = useBookings(userId);

await createBooking({
  artistId: 'artist-uuid',
  eventType: 'Свадьба',
  eventDate: '2024-06-15',
  totalPrice: 200000
});
```

#### `useChat(userId, bookingId)` - Чат
```typescript
const { messages, sendMessage, markAsRead, unreadCount } = useChat(userId, bookingId);

await sendMessage({
  recipientId: 'artist-uuid',
  message: 'Здравствуйте! Хочу обсудить детали.'
});
```

#### `useArtistReviews(artistId)` - Отзывы
```typescript
const { reviews, averageRating, totalReviews } = useArtistReviews(artistId);
```

#### `useCreateReview(userId)` - Создание отзыва
```typescript
const { createReview } = useCreateReview(userId);

await createReview({
  bookingId: 'booking-uuid',
  artistId: 'artist-uuid',
  rating: 5,
  reviewText: 'Отличный артист!',
  wouldRecommend: true
});
```

---

## 🎨 Компоненты

### `<DatabaseSeedPanel />` - Админ-панель
Управление загрузкой артистов в базу данных.

### `<ChatWindow />` - Окно чата
Realtime чат между заказчиком и артистом.

### `<ReviewsSection />` - Отзывы
Отображение и управление отзывами.

---

## 🔐 Row Level Security (RLS)

Все таблицы защищены RLS политиками:

- **artists_catalog**: публичный просмотр
- **bookings**: видны только участникам
- **chat_messages**: только отправитель и получатель
- **reviews**: публичный просмотр, создание только для завершённых бронирований

---

## 🚀 Следующие шаги

### Готово к продакшену:
- ✅ База данных настроена
- ✅ 180 артистов загружены
- ✅ Бронирования работают
- ✅ Чат в реальном времени
- ✅ Система отзывов

### Можно добавить:
- 💳 Интеграция платёжных систем (Kaspi, PayBox, Stripe)
- 🤖 AI-рекомендации через OpenAI API
- 📧 Email-уведомления через Supabase Functions
- 📱 Push-уведомления
- 📊 Аналитика и статистика
- 🔍 Полнотекстовый поиск

---

## 📞 Техподдержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте Supabase Dashboard → Logs
3. Убедитесь, что все миграции выполнены

---

🎉 **Поздравляем! TuranSound полностью готов к работе!**
