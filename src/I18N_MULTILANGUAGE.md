# 🌍 Система многоязычности TuranSound

**Дата:** 16 января 2026  
**Статус:** ✅ Реализовано  
**Языки:** Казахский (kk), Русский (ru), Английский (en)

---

## 📚 Обзор

TuranSound теперь поддерживает **три языка**:
- 🇰🇿 **Қазақша** (Казахский) - `kk`
- 🇷🇺 **Русский** - `ru` (язык по умолчанию)
- 🇬🇧 **English** (Английский) - `en`

### Функции:
- ✅ Автоопределение языка браузера
- ✅ Сохранение выбранного языка в localStorage
- ✅ Переключатель языка в Header
- ✅ React Context API для управления
- ✅ TypeScript поддержка
- ✅ SEO-оптимизация (lang attribute)

---

## 📁 Структура файлов

```
/lib/i18n/
├── translations.ts        # Все переводы
└── LanguageContext.tsx    # React Context для языка

/components/
└── LanguageSwitcher.tsx   # Компонент переключателя языка
```

---

## 🎯 Использование

### 1. В компонентах

```tsx
import { useTranslation } from '../lib/i18n/LanguageContext';

function MyComponent() {
  const { t, language, setLanguage } = useTranslation();

  return (
    <div>
      <h1>{t.header.home}</h1>
      <p>{t.common.loading}</p>
      <button>{t.catalog.searchPlaceholder}</button>
    </div>
  );
}
```

### 2. Переключение языка

```tsx
const { setLanguage } = useTranslation();

// Установить казахский
setLanguage('kk');

// Установить русский
setLanguage('ru');

// Установить английский
setLanguage('en');
```

### 3. Текущий язык

```tsx
const { language } = useTranslation();

console.log(language); // 'kk' | 'ru' | 'en'
```

---

## 📖 Структура переводов

### Категории:

#### 1. **common** - Общие фразы
```tsx
t.common.loading        // "Загрузка..." / "Жүктелуде..." / "Loading..."
t.common.save           // "Сохранить" / "Сақтау" / "Save"
t.common.cancel         // "Отмена" / "Болдырмау" / "Cancel"
```

#### 2. **header** - Навигация
```tsx
t.header.home           // "Главная" / "Басты бет" / "Home"
t.header.catalog        // "Каталог" / "Каталог" / "Catalog"
t.header.aiAssistant    // "AI-Помощник" / "AI-Көмекші" / "AI Assistant"
```

#### 3. **home** - Главная страница
```tsx
t.home.heroTitle        // "Музыкальная платформа ЦА"
t.home.browseArtists    // "Смотреть артистов"
```

#### 4. **catalog** - Каталог
```tsx
t.catalog.title         // "Каталог артистов"
t.catalog.filters       // "Фильтры"
t.catalog.searchPlaceholder // "Поиск по имени артиста..."
```

#### 5. **artist** - Профиль артиста
```tsx
t.artist.about          // "О себе"
t.artist.portfolio      // "Портфолио"
t.artist.bookNow        // "Забронировать"
```

#### 6. **booking** - Бронирование
```tsx
t.booking.title         // "Бронирование"
t.booking.confirmBooking // "Подтвердить бронирование"
```

#### 7. **footer** - Футер
```tsx
t.footer.description    // "Цифровая платформа..."
t.footer.contacts       // "Контакты"
```

#### 8. **languages** - Названия языков
```tsx
t.languages.kazakh      // "Қазақша"
t.languages.russian     // "Русский"
t.languages.english     // "English"
```

#### 9. **roles** - Роли артистов
```tsx
t.roles.singer          // "Певец/певица" / "Әнші" / "Singer"
t.roles.dj              // "DJ"
t.roles.mc              // "Тамада" / "Тамада" / "MC/Toastmaster"
```

#### 10. **genres** - Музыкальные жанры
```tsx
t.genres.pop            // "Поп" / "Поп" / "Pop"
t.genres.jazz           // "Джаз" / "Джаз" / "Jazz"
t.genres.traditional    // "Традиционная" / "Дәстүрлі" / "Traditional"
```

---

## 🔧 Добавление новых переводов

### Шаг 1: Обновить интерфейс в `/lib/i18n/translations.ts`

```typescript
export interface Translations {
  // ... существующие
  
  // Новая категория
  newCategory: {
    newKey: string;
    anotherKey: string;
  };
}
```

### Шаг 2: Добавить переводы для всех языков

```typescript
export const translations: Record<Language, Translations> = {
  kk: {
    // ... существующие
    newCategory: {
      newKey: 'Мәтін қазақ тілінде',
      anotherKey: 'Тағы бір мәтін',
    },
  },
  
  ru: {
    // ... существующие
    newCategory: {
      newKey: 'Текст на русском',
      anotherKey: 'Ещё один текст',
    },
  },
  
  en: {
    // ... существующие
    newCategory: {
      newKey: 'Text in English',
      anotherKey: 'Another text',
    },
  },
};
```

### Шаг 3: Использовать в компоненте

```tsx
const { t } = useTranslation();

<h1>{t.newCategory.newKey}</h1>
```

---

## 🎨 Компонент LanguageSwitcher

Переключатель языка находится в Header справа:

```tsx
<LanguageSwitcher />
```

### Внешний вид:
- **Desktop:** 🇰🇿 Қазақша (флаг + название)
- **Mobile:** 🇰🇿 (только флаг)
- **Dropdown:** Список всех языков
- **Активный язык:** Выделен фиолетовым фоном

---

## 💾 Хранение выбранного языка

### localStorage
Выбранный язык сохраняется в `localStorage`:

```javascript
localStorage.getItem('turansound_language') // 'kk' | 'ru' | 'en'
```

### Автоопределение
При первом посещении:
1. Проверяется localStorage
2. Если нет - определяется язык браузера
3. Если не поддерживается - используется русский (ru)

```typescript
// Примеры определения:
navigator.language = 'kk-KZ' → язык = 'kk'
navigator.language = 'ru-RU' → язык = 'ru'
navigator.language = 'en-US' → язык = 'en'
navigator.language = 'fr-FR' → язык = 'ru' (по умолчанию)
```

---

## 🔍 SEO оптимизация

HTML `lang` attribute автоматически обновляется:

```html
<html lang="kk">  <!-- Когда выбран казахский -->
<html lang="ru">  <!-- Когда выбран русский -->
<html lang="en">  <!-- Когда выбран английский -->
```

---

## 📱 Где используется

### Уже переведено:
- ✅ Header (навигация, кнопки)
- ✅ Footer (полностью)
- ✅ LanguageSwitcher

### Требует перевода:
- ⏳ HomePage
- ⏳ CatalogPage
- ⏳ ProductionPage
- ⏳ AIAssistant
- ⏳ ArtistProfile
- ⏳ BookingModal
- ⏳ LoginPage / RegisterPage
- ⏳ Dashboard
- ⏳ И другие страницы...

---

## 🚀 Следующие шаги

### 1. Перевести все страницы

Заменить хардкодные тексты на `t.category.key`:

```tsx
// ❌ Плохо
<h1>Добро пожаловать</h1>

// ✅ Хорошо
const { t } = useTranslation();
<h1>{t.home.welcome}</h1>
```

### 2. Добавить переводы для форм

```tsx
t.forms.emailPlaceholder
t.forms.passwordPlaceholder
t.forms.submitButton
```

### 3. Перевести сообщения об ошибках

```tsx
t.errors.required
t.errors.invalidEmail
t.errors.networkError
```

### 4. Перевести уведомления (toast)

```tsx
toast.success(t.notifications.bookingSuccess);
toast.error(t.notifications.bookingError);
```

---

## 📝 Пример полного компонента

```tsx
import { useTranslation } from '../lib/i18n/LanguageContext';
import { Button } from './ui/button';

export function ExamplePage() {
  const { t, language, setLanguage } = useTranslation();

  return (
    <div>
      {/* Заголовок */}
      <h1>{t.catalog.title}</h1>
      <p>{t.catalog.subtitle}</p>

      {/* Кнопки */}
      <Button>{t.common.search}</Button>
      <Button variant="outline">{t.common.cancel}</Button>

      {/* Условный рендеринг */}
      {language === 'kk' && (
        <div>Қазақ тіліндегі ерекше контент</div>
      )}

      {/* Переключение языка программно */}
      <button onClick={() => setLanguage('en')}>
        Switch to English
      </button>
    </div>
  );
}
```

---

## 🎯 Best Practices

### 1. Используйте t. для всех текстов
```tsx
// ✅ Правильно
{t.header.home}

// ❌ Неправильно
{"Главная"}
```

### 2. Группируйте переводы логически
```tsx
t.booking.title         // Заголовки бронирования
t.booking.confirmButton // Кнопки бронирования
t.booking.status.pending // Статусы бронирования
```

### 3. Используйте TypeScript
TypeScript автоматически подскажет доступные ключи:
```tsx
t.catalog.   // автодополнение покажет все ключи
```

### 4. Короткие, понятные ключи
```tsx
// ✅ Хорошо
t.common.save
t.header.home

// ❌ Плохо
t.commonButtons.saveButtonText
t.navigationHeader.homePageLink
```

---

## 🐛 Отладка

### Проверить текущий язык:
```tsx
const { language } = useTranslation();
console.log('Current language:', language);
```

### Проверить localStorage:
```javascript
console.log(localStorage.getItem('turansound_language'));
```

### Сбросить язык:
```javascript
localStorage.removeItem('turansound_language');
window.location.reload();
```

---

## 📊 Статистика переводов

### Текущий охват:
- **Категорий**: 10
- **Всего ключей**: ~150+
- **Языков**: 3
- **Переведённых строк**: ~450+

### Прогресс по компонентам:
- ✅ Header: 100%
- ✅ Footer: 100%
- ✅ LanguageSwitcher: 100%
- ⏳ HomePage: 0%
- ⏳ CatalogPage: 0%
- ⏳ Остальное: 0%

---

## 🌟 Особенности реализации

### 1. React Context API
Легковесное решение без дополнительных библиотек

### 2. TypeScript Safety
Полная типизация всех переводов

### 3. Performance
Минимальный оверхед, мгновенное переключение

### 4. User Experience
- Автоопределение языка
- Сохранение выбора
- Плавное переключение

---

## 🔗 Файлы системы

```
/lib/i18n/
├── translations.ts          # 450+ строк переводов
└── LanguageContext.tsx      # Context + hooks (80 строк)

/components/
└── LanguageSwitcher.tsx     # UI компонент (50 строк)

/App.tsx                     # LanguageProvider обёртка
/components/Header.tsx       # Использование переводов
```

---

## 📚 Справочник команд

### Получить текущий язык:
```tsx
const { language } = useTranslation();
```

### Изменить язык:
```tsx
const { setLanguage } = useTranslation();
setLanguage('kk');  // Казахский
setLanguage('ru');  // Русский
setLanguage('en');  // Английский
```

### Получить переводы:
```tsx
const { t } = useTranslation();
console.log(t.header.home);
```

### Полный пример:
```tsx
const { t, language, setLanguage } = useTranslation();
```

---

**Готово к использованию! 🎉**

*Система многоязычности полностью настроена и готова для расширения на все компоненты платформы.*
