# 🏗️ Трехслойная архитектура TuranSound

## 📋 Обзор

TuranSound использует трехслойную архитектуру каталога для эффективной категоризации и поиска артистов. Архитектура разделена на три логических слоя:

1. **СЛОЙ 1: КТО ЭТО** - Основные разделы каталога
2. **СЛОЙ 2: ЧТО ОНИ ДЕЛАЮТ** - Роли и специализации
3. **СЛОЙ 3: СТИЛИ И ФОРМАТЫ** - Жанры, национальные стили, форматы мероприятий

---

## 🎯 СЛОЙ 1: РАЗДЕЛЫ (КТО ЭТО)

Это основные категории каталога — крупные кнопки на главной странице, помогающие пользователям быстро найти нужный тип артиста.

### Доступные разделы:

#### 🎤 Артисты сцены (`stage_artists`)
Музыканты, DJ, ансамбли, оркестры, фольклорные артисты, караоке-ведущие

#### 🎭 Ведущие и шоу (`hosts_and_shows`)
Тамада, шоумены, ведущие мероприятий

#### 🎬 Креатив и продакшн (`creative_production`)
Композиторы, поэты песен, саунд-дизайнеры, сценаристы, режиссёры, актёры

### UI-дизайн:
- Крупные карточки с иконками
- Градиентные фоны (purple-pink)
- Hover-эффекты с анимацией

### Код:
```typescript
type ArtistSection = 
  | 'stage_artists'
  | 'hosts_and_shows'
  | 'creative_production';
```

---

## ⚙️ СЛОЙ 2: РОЛИ (ЧТО ОНИ ДЕЛАЮТ)

Один артист может иметь несколько ролей. Роли определяют функциональность артиста и используются для:
- Фильтрации в каталоге
- AI-подбора
- Контрактов и бронирований

### Музыкальные роли:
- `musician` - Исполнитель
- `dj` - DJ
- `ensemble` - Ансамбль
- `orchestra` - Оркестр
- `folk_artist` - Фольклорный артист
- `karaoke_host` - Караоке-ведущий
- `composer` - Композитор
- `arranger` - Аранжировщик
- `lyricist` - Поэт песен
- `sound_designer` - Саунд-дизайнер

### Ивент-роли:
- `tamada` - Тамада
- `host` - Ведущий
- `showman` - Шоумен

### Продакшн-роли:
- `actor` - Актёр
- `director` - Режиссёр
- `ad_director` - Режиссёр рекламы
- `screenwriter` - Сценарист

### UI-дизайн:
- Чекбоксы с множественным выбором
- Группировка по разделам
- Фильтрация доступных ролей в зависимости от выбранного раздела

### Код:
```typescript
type ArtistRole =
  | 'musician' | 'dj' | 'ensemble' | 'orchestra'
  | 'folk_artist' | 'karaoke_host' | 'composer'
  | 'arranger' | 'lyricist' | 'sound_designer'
  | 'tamada' | 'host' | 'showman'
  | 'actor' | 'director' | 'ad_director' | 'screenwriter';

// Роли по разделам
const ROLES_BY_SECTION: Record<ArtistSection, ArtistRole[]> = {
  stage_artists: ['musician', 'dj', 'ensemble', ...],
  hosts_and_shows: ['tamada', 'host', 'showman'],
  creative_production: ['composer', 'arranger', ...],
};
```

---

## 🎨 СЛОЙ 3: СТИЛИ И ФОРМАТЫ (КАК ЗВУЧИТ / ВЫГЛЯДИТ)

Жанры и стили не привязаны напрямую к разделам — это гибкие теги, которые можно комбинировать.

### Музыкальные жанры (`MusicGenre`):
Pop, Rock, Jazz, Classical, Hip-hop, Electronic, Folk, Traditional, Blues, Country, Reggae, Latin

### Национальные стили (`NationalStyle`):
- `dombra` - Домбра
- `kobyz` - Кобыз
- `ethno` - Этно
- `kazakh_traditional` - Казахская традиционная
- `uzbek_traditional` - Узбекская традиционная
- `kyrgyz_traditional` - Кыргызская традиционная
- `turkmen_traditional` - Туркменская традиционная

### Форматы мероприятий (`EventFormat`):
Wedding, Corporate, Festival, Private event, Concert, Club, Restaurant, Birthday, Anniversary

### UI-дизайн:
- **Жанры** → Бейджи (chips) с градиентом purple-pink
- **Национальные стили** → Бейджи с градиентом violet-purple
- **Форматы мероприятий** → Бейджи с градиентом pink-rose
- Возможность множественного выбора

---

## 🧭 Пользовательский опыт (UX)

### 1. Главная страница
Пользователь видит три крупные карточки:
- 🎤 Артисты сцены
- 🎭 Ведущие и шоу
- 🎬 Креатив и продакшн

### 2. Каталог с фильтрами
После выбора раздела пользователь может:
1. Выбрать роли (чекбоксы)
2. Добавить жанры (теги)
3. Указать национальные стили (теги)
4. Выбрать форматы мероприятий (теги)
5. Фильтровать по региону, цене, рейтингу

### 3. Пример пути пользователя
**Задача:** Найти музыканта на свадьбу в этно-стиле

1. Нажать **"Артисты сцены"**
2. Выбрать роль **"Музыкант"**
3. Жанры: **Folk + Traditional**
4. Национальный стиль: **Dombra**
5. Формат: **Wedding**

---

## 📦 Структура данных

### Интерфейс Artist:

```typescript
interface Artist {
  id: string;
  name: string;
  stageName: string;
  avatar: string;
  city: string;
  region: string;
  
  // СЛОЙ 1: Раздел (один)
  section: ArtistSection;
  
  // СЛОЙ 2: Роли (массив - может быть несколько)
  roles: ArtistRole[];
  
  // СЛОЙ 3: Жанры и стили (массивы)
  genres: MusicGenre[];
  nationalStyles?: NationalStyle[];
  eventFormats: EventFormat[];
  
  // Портфолио
  bio: string;
  experience: number;
  videos?: string[];
  photos?: string[];
  audio?: string[];
  
  // Финансы
  priceFrom: number;
  priceTo?: number;
  currency: 'KZT' | 'USD';
  
  // Репутация
  rating: number;
  reviewCount: number;
  bookingCount: number;
  
  // Даты
  createdAt: string;
  isVerified: boolean;
  isAvailable: boolean;
}
```

---

## 🎨 Дизайн-правила

### Цветовые градиенты:
- **Артисты сцены**: `from-purple-500 to-pink-500`
- **Ведущие и шоу**: `from-pink-500 to-rose-500`
- **Креатив и продакшн**: `from-violet-500 to-purple-500`

### Компоненты:
- **Разделы** → Крупные иконки с градиентным фоном
- **Роли** → Чекбоксы в карточках
- **Жанры** → Теги (Badge) с hover-эффектами
- **Форматы** → Цветные бейджи

### Принципы:
1. ✅ Красиво
2. ✅ Масштабируемо
3. ✅ Привычно пользователю
4. ✅ Семантически правильно

---

## 🔧 Файловая структура

```
/types/artist.ts           # Все типы трехслойной архитектуры
/components/
  SectionSelector.tsx      # Выбор раздела на главной
  CatalogFilters.tsx       # Трехслойные фильтры
  ArtistOnboarding.tsx     # Onboarding для артистов
  CatalogBreadcrumbs.tsx   # Навигация по каталогу
/pages/
  HomePage.tsx             # Главная с разделами
  CatalogPage.tsx          # Каталог с фильтрацией
```

---

## 🚀 Следующие шаги

### Критически важно:
1. ✅ Трехслойная типизация (`/types/artist.ts`) - **ГОТОВО**
2. ✅ Компонент выбора разделов (`SectionSelector`) - **ГОТОВО**
3. ✅ Onboarding для артистов (`ArtistOnboarding`) - **ГОТОВО**
4. ✅ Обновлённые фильтры (`CatalogFilters`) - **ГОТОВО**
5. ✅ Интеграция в каталог (`CatalogPage`) - **ГОТОВО**
6. 🔄 Обновить mockData для использования новых типов - **В РАБОТЕ**
7. ⏳ Интеграция с Supabase (сохранение профилей артистов)
8. ⏳ AI-подбор на основе трехслойной архитектуры
9. ⏳ Смарт-рекомендации

---

## 📊 База данных (Supabase)

### Таблица `artists`:
```sql
CREATE TABLE artists (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES auth.users,
  
  -- СЛОЙ 1
  section TEXT NOT NULL CHECK (section IN ('stage_artists', 'hosts_and_shows', 'creative_production')),
  
  -- СЛОЙ 2
  roles TEXT[] NOT NULL, -- массив ролей
  
  -- СЛОЙ 3
  genres TEXT[],
  national_styles TEXT[],
  event_formats TEXT[],
  
  -- Остальные поля
  ...
);
```

---

## 💡 Преимущества архитектуры

### 1. Гибкость
Артист может быть музыкантом + композитором + саунд-дизайнером

### 2. Масштабируемость
Легко добавлять новые роли, жанры, форматы

### 3. SEO-оптимизация
Семантическая структура улучшает индексацию

### 4. UX
Пользователь быстро находит нужного артиста через интуитивную фильтрацию

### 5. AI-friendly
Структурированные данные идеальны для ML-рекомендаций

---

*Документация обновлена: 14 января 2026*
