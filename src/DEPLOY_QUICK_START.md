# ⚡ Быстрый старт деплоя (5 минут)

## Вариант 1: Vercel (Рекомендуется)

### 1️⃣ Подготовка (1 мин)
```bash
# В папке проекта
git init
git add .
git commit -m "Initial commit"
```

### 2️⃣ GitHub (1 мин)
1. Создайте репозиторий: https://github.com/new
2. Назовите: `turansound`
3. Выполните:
```bash
git remote add origin https://github.com/ваш-username/turansound.git
git branch -M main
git push -u origin main
```

### 3️⃣ Vercel (2 мин)
1. Откройте: https://vercel.com/new
2. Войдите через GitHub
3. Импортируйте репозиторий `turansound`
4. Добавьте переменные окружения:
   - `VITE_SUPABASE_PROJECT_ID` = `hpcwkbkglggimwcbxpoh`
   - `VITE_SUPABASE_ANON_KEY` = (ваш ключ из Supabase)
5. Нажмите **Deploy**

### 4️⃣ Готово! (1 мин)
- Получите ссылку: `https://turansound.vercel.app`
- Проверьте работу сайта
- Настройте домен (опционально)

---

## Где взять Supabase ключи?

1. Откройте: https://app.supabase.com
2. Выберите ваш проект
3. **Settings → API**
4. Скопируйте:
   - Project URL → возьмите ID из URL (`hpcwkbkglggimwcbxpoh`)
   - anon public key → полностью скопируйте

---

## Вариант 2: Netlify

```bash
# Установите Netlify CLI
npm install -g netlify-cli

# Деплой
netlify deploy --prod

# Или через веб-интерфейс:
# 1. https://app.netlify.com/drop
# 2. Перетащите папку 'dist' (после npm run build)
```

---

## Проверка после деплоя

✅ Откройте сайт  
✅ Зайдите в каталог  
✅ Попробуйте регистрацию  
✅ Проверьте AI-ассистента  

Если что-то не работает → откройте `/DEPLOYMENT_GUIDE.md`

---

## Автоматическое обновление

После деплоя каждый `git push` автоматически обновляет сайт!

```bash
# Внесли изменения
git add .
git commit -m "Обновление"
git push

# Vercel автоматически деплоит за 2-3 минуты
```

---

## Нужна помощь?

📖 Полная инструкция: `/DEPLOYMENT_GUIDE.md`  
🔧 Устранение проблем: раздел "Troubleshooting" в инструкции  
📊 Проверка логов: Vercel/Netlify Dashboard → Deployments → Logs
