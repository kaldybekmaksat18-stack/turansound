# 🚀 Руководство по настройке Supabase для TuranSound

## ✅ Исправленная проблема

**Ошибка:** `⚠️ Таблица artist_profiles не найдена`

**Причина:** В файле миграции была синтаксическая ошибка (лишний символ `=` на строке 11)

**Статус:** ✅ **ИСПРАВЛЕНО**

---

## 📋 Пошаговая инструкция

### Шаг 1: Откройте Supabase Dashboard

1. Перейдите по ссылке: https://supabase.com/dashboard/project/hpcwkbkglggimwcbxpoh/editor
2. Войдите в свой аккаунт Supabase

### Шаг 2: Откройте SQL Editor

1. В левом меню нажмите **"SQL Editor"**
2. Нажмите **"New Query"** (Новый запрос)

### Шаг 3: Скопируйте миграцию

1. Откройте файл `/supabase/migrations/000_COMPLETE_SETUP.sql` в вашем проекте
2. **Скопируйте ВСЁ содержимое файла** (Ctrl+A → Ctrl+C)
3. Вставьте в SQL Editor в Supabase (Ctrl+V)

### Шаг 4: Выполните миграцию

1. Нажмите кнопку **"Run"** (Выполнить) в правом нижнем углу
2. Дождитесь завершения выполнения
3. Вы должны увидеть сообщение: ✅ **"Success. No rows returned"**

### Шаг 5: Проверьте результат

1. В левом меню перейдите в **"Table Editor"**
2. Убедитесь, что созданы следующие таблицы:
   - ✅ `artist_profiles` - Профили артистов
   - ✅ `customer_profiles` - Профили заказчиков
   - ✅ `artists_catalog` - Каталог артистов
   - ✅ `bookings` - Бронирования
   - ✅ `messages` - Сообщения чата
   - ✅ `reviews` - Отзывы

---

## 🎯 Что создает миграция

### 📊 Таблицы (6 таблиц)

#### 1. **artist_profiles** - Профили артистов
```sql
- id (UUID) - уникальный идентификатор
- user_id (UUID) - связь с auth.users
- stage_name - сценическое имя
- city - город
- genres[] - жанры
- bio - биография
- verified - верифицирован ли
- experience - опыт (лет)
- base_price - базовая цена
- videos, photos, audio_tracks - портфолио
```

#### 2. **customer_profiles** - Профили заказчиков
```sql
- id (UUID) - уникальный идентификатор
- user_id (UUID) - связь с auth.users
- full_name - полное имя
- company_name - название компании
- city - город
- event_preferences[] - предпочтения
```

#### 3. **artists_catalog** - Каталог артистов (180 реальных)
```sql
- id (UUID)
- section - раздел (stage_artists, hosts_and_shows, creative_production)
- roles[] - роли
- genres[] - жанры
- price_from / price_to - диапазон цен
- rating - рейтинг (0-5)
- is_verified - верифицирован
```

#### 4. **bookings** - Система бронирования
```sql
- id (UUID)
- customer_id, artist_id - участники
- event_date, event_time - дата и время
- total_price - цена
- escrow_amount - эскроу
- status - статус (pending, confirmed, completed, etc.)
- contract_signed - подписан ли контракт
```

#### 5. **messages** - Чат
```sql
- id (UUID)
- booking_id - связь с бронированием
- sender_id, receiver_id - отправитель/получатель
- message - текст сообщения
- read - прочитано
```

#### 6. **reviews** - Отзывы
```sql
- id (UUID)
- artist_id - артист
- customer_id - заказчик
- rating (1-5) - оценка
- comment - комментарий
```

### 🔐 RLS Политики (Row Level Security)

**artist_profiles:**
- ✅ Все могут читать профили артистов
- ✅ Пользователи могут создавать/обновлять только свой профиль

**customer_profiles:**
- ✅ Все могут читать профили заказчиков
- ✅ Пользователи могут создавать/обновлять только свой профиль

**artists_catalog:**
- ✅ Все могут читать каталог

**bookings:**
- ✅ Пользователи видят только свои бронирования
- ✅ Артисты видят бронирования, где они исполнители

**messages:**
- ✅ Пользователи видят только свои сообщения

**reviews:**
- ✅ Все могут читать отзывы
- ✅ Заказчики могут создавать/обновлять/удалять свои отзывы

### 🎨 Индексы для производительности

- ✅ Индексы по `user_id` для быстрого поиска профилей
- ✅ GIN индексы для массивов (genres, roles, event_formats)
- ✅ Индексы по датам и статусам
- ✅ Индексы по рейтингу и цене

### ⚡ Триггеры

- ✅ Автоматическое обновление `updated_at` при изменении записей
- ✅ Автоматический пересчет рейтинга артиста при новом отзыве

---

## 🔧 Альтернативные варианты установки

### Вариант A: Пошаговая установка (рекомендуется для новичков)

Вместо файла `000_COMPLETE_SETUP.sql` можно выполнить миграции по порядку:

1. **001_create_profiles.sql** - Профили артистов и заказчиков
2. **002_artists_catalog_and_bookings.sql** - Каталог и бронирования
3. **003_fix_rls_policies.sql** - Исправление RLS политик

### Вариант B: Через Supabase CLI (для продвинутых)

```bash
# Установите Supabase CLI
npm install -g supabase

# Войдите в аккаунт
supabase login

# Свяжите проект
supabase link --project-ref hpcwkbkglggimwcbxpoh

# Примените миграции
supabase db push
```

---

## 🎯 Загрузка данных артистов (180 казахстанских артистов)

После выполнения миграции нужно загрузить данные артистов:

### Способ 1: Через Supabase Dashboard (рекомендуется)

1. Откройте **Table Editor** → `artists_catalog`
2. Нажмите **"Insert row"** → **"Insert from CSV"**
3. Загрузите файл `/data/artists-catalog.json` (конвертируйте в CSV)

### Способ 2: Через SQL

1. Откройте **SQL Editor**
2. Скопируйте содержимое файла `/data/insert_artists.sql`
3. Нажмите **"Run"**

### Способ 3: Через Admin Panel (в разработке)

Платформа будет иметь Admin Panel для массовой загрузки артистов.

---

## ❗ Частые проблемы и решения

### Проблема 1: "permission denied for schema public"

**Решение:**
```sql
-- Выполните в SQL Editor:
GRANT ALL ON SCHEMA public TO postgres;
GRANT ALL ON SCHEMA public TO anon;
GRANT ALL ON SCHEMA public TO authenticated;
```

### Проблема 2: "relation already exists"

**Решение:**
Таблицы уже созданы. Можно:
1. Пропустить этот шаг
2. Или удалить таблицы и создать заново:
```sql
DROP TABLE IF EXISTS public.reviews CASCADE;
DROP TABLE IF EXISTS public.messages CASCADE;
DROP TABLE IF EXISTS public.bookings CASCADE;
DROP TABLE IF EXISTS public.artists_catalog CASCADE;
DROP TABLE IF EXISTS public.customer_profiles CASCADE;
DROP TABLE IF EXISTS public.artist_profiles CASCADE;
```

### Проблема 3: "function update_updated_at_column already exists"

**Решение:**
Функция уже создана. Можно пропустить или использовать `CREATE OR REPLACE FUNCTION`.

### Проблема 4: "No rows returned"

**Это нормально!** ✅ Это означает, что миграция выполнена успешно.

---

## 🔍 Проверка работоспособности

### Тест 1: Проверка таблиц

```sql
-- Выполните в SQL Editor:
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
```

**Ожидаемый результат:**
```
artist_profiles
artists_catalog
bookings
customer_profiles
messages
reviews
```

### Тест 2: Проверка RLS политик

```sql
-- Проверка политик для artist_profiles:
SELECT * FROM pg_policies 
WHERE tablename = 'artist_profiles';
```

### Тест 3: Создание тестового профиля

```sql
-- ВАЖНО: Замените 'your-user-id' на реальный UUID из auth.users
INSERT INTO public.artist_profiles (user_id, stage_name, city)
VALUES ('your-user-id', 'Test Artist', 'Алматы');
```

---

## 📚 Документация

### Структура проекта
```
/supabase/
  migrations/
    000_COMPLETE_SETUP.sql       ← Основная миграция (ИСПОЛЬЗУЙТЕ ЭТУ)
    001_create_profiles.sql      ← Профили (альтернатива)
    002_artists_catalog_and_bookings.sql  ← Каталог (альтернатива)
    003_fix_rls_policies.sql     ← RLS (альтернатива)
```

### Связанные файлы
- `/utils/supabase/client.ts` - Клиент Supabase с хелперами
- `/hooks/useArtistProfile.ts` - Хук для работы с профилями артистов
- `/hooks/useCustomerProfile.ts` - Хук для работы с профилями заказчиков
- `/data/artists.ts` - 180 реальных казахстанских артистов

---

## 🎉 После успешной установки

Платформа теперь поддерживает:

✅ **Регистрацию артистов и заказчиков**
✅ **Каталог с 180 реальными артистами**
✅ **Систему бронирования**
✅ **Эскроу-счета**
✅ **Чат между артистами и заказчиками**
✅ **Систему отзывов и рейтинга**
✅ **Смарт-контракты**
✅ **RLS безопасность**

Следующий шаг: **Загрузите данные артистов** и протестируйте платформу!

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте раздел **"Частые проблемы"** выше
2. Откройте консоль браузера (F12) для просмотра ошибок
3. Проверьте логи в Supabase Dashboard → Logs

---

**Дата:** 17 января 2026  
**Версия миграции:** 1.0 (исправленная)  
**Статус:** ✅ Готово к использованию
