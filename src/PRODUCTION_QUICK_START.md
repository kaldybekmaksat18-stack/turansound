# 🎬 Быстрый старт: Продакшн-артисты

## Что добавлено?

В платформу TuranSound добавлено **20 известных казахстанских продакшн-специалистов**:

- ✅ **5 актёров** театра и кино
- ✅ **5 режиссёров** с фестивальным опытом
- ✅ **5 режиссёров рекламы** для коммерческих проектов
- ✅ **5 сценаристов** для кино, театра и ТВ

---

## 🚀 Как получить доступ

### 1️⃣ Через главную страницу
На главной странице появился новый баннер **"База продакшн-специалистов"**:
- Кнопка **"Смотреть всех артистов"** → витрина всех 20 специалистов
- Кнопка **"В каталог"** → каталог с фильтром по разделу "Креатив и продакшн"

### 2️⃣ Через навигацию
В главном меню появился пункт **"🎬 Продакшн"**:
- Переход на специальную витрину продакшн-артистов
- Группировка по категориям (актёры, режиссёры, и т.д.)
- Статистика и детальная информация

### 3️⃣ Через каталог
Выберите раздел **"Креатив и продакшн"**:
- Используйте фильтры по ролям
- Сортировка по опыту, рейтингу, цене
- Быстрый поиск по имени

### 4️⃣ Через код

```typescript
// Импорт всех продакшн-артистов
import { productionArtists } from './data/productionArtists';

// Или по категориям
import { 
  actors,           // 5 актёров
  directors,        // 5 режиссёров
  adDirectors,      // 5 режиссёров рекламы
  screenwriters     // 5 сценаристов
} from './data/productionArtists';

// Через общий файл
import { allKazakhArtists } from './data/allKazakhArtists';
// prodution artists включены в allKazakhArtists
```

---

## 📁 Структура файлов

```
/data/
  ├── productionArtists.ts     ← НОВЫЙ! 20 продакшн-специалистов
  ├── allKazakhArtists.ts      ← Обновлён: включает productionArtists
  ├── kazakhArtists.ts
  ├── kazakhArtistsPart2.ts
  └── kazakhArtistsPart3.ts

/components/
  └── ProductionShowcase.tsx   ← НОВЫЙ! Витрина продакшн-артистов

/pages/
  ├── HomePage.tsx             ← Обновлён: баннер продакшн-артистов
  ├── CatalogPage.tsx
  └── ProductionPage.tsx       ← НОВЫЙ! Страница продакшн-витрины

/types/
  └── artist.ts                ← Трехслойная архитектура
```

---

## 🎯 Роли продакшн-артистов

Все артисты имеют правильную типизацию:

```typescript
// СЛОЙ 1: Раздел
section: 'creative_production'

// СЛОЙ 2: Роли
roles: ['actor']        // Актёр
roles: ['director']     // Режиссёр
roles: ['ad_director']  // Режиссёр рекламы
roles: ['screenwriter'] // Сценарист

// Некоторые имеют множественные роли:
roles: ['director', 'screenwriter']  // Ермек Шинарбаев
roles: ['ad_director', 'director']   // Азат Байжанов

// СЛОЙ 3: Форматы мероприятий
eventFormats: ['corporate', 'festival', 'private_event', ...]
```

---

## 🔍 Примеры использования

### Фильтрация по ролям

```typescript
// Найти всех актёров
const actors = allKazakhArtists.filter(
  artist => artist.roles.includes('actor')
);

// Найти режиссёров рекламы в Алматы
const almatyAdDirectors = allKazakhArtists.filter(
  artist => 
    artist.roles.includes('ad_director') && 
    artist.city === 'Алматы'
);

// Найти специалистов с опытом > 20 лет
const experienced = productionArtists.filter(
  artist => artist.experience > 20
);
```

### Доступ к статистике

```typescript
import { productionStats } from './data/productionArtists';

console.log(productionStats.actors.count);        // 5
console.log(productionStats.directors.name);      // "Режиссёры"
console.log(productionStats.screenwriters.icon);  // "✍️"
```

### Работа с данными

```typescript
// Сортировка по опыту
const sortedByExperience = [...productionArtists].sort(
  (a, b) => b.experience - a.experience
);

// Сортировка по цене
const sortedByPrice = [...productionArtists].sort(
  (a, b) => a.priceFrom - b.priceFrom
);

// Группировка по городам
const byCity = productionArtists.reduce((acc, artist) => {
  if (!acc[artist.city]) acc[artist.city] = [];
  acc[artist.city].push(artist);
  return acc;
}, {} as Record<string, typeof productionArtists>);
```

---

## ✨ Особенности данных

### Все артисты верифицированы
```typescript
isVerified: true   // 100% верификация
isAvailable: true  // Все доступны для заказов
```

### Реальные данные
- Опыт от 8 до 45 лет
- Цены от 1.2М до 12М KZT
- Рейтинги от 4.75 до 5.0
- Реальные города Казахстана

### Полные профили
Каждый артист включает:
- ✅ Биографию с достижениями
- ✅ Известные работы
- ✅ Форматы мероприятий
- ✅ Ценовой диапазон
- ✅ Рейтинг и отзывы
- ✅ Опыт работы

---

## 📊 Статистика

### По категориям
| Категория | Количество | Средний опыт | Средняя цена (от) |
|-----------|-----------|--------------|-------------------|
| 🎭 Актёры | 5 | 23 года | 1.7M KZT |
| 🎬 Режиссёры | 5 | 29 лет | 3.8M KZT |
| 📹 Режиссёры рекламы | 5 | 13.6 лет | 1.6M KZT |
| ✍️ Сценаристы | 5 | 22 года | 2.3M KZT |

### По городам
- **Алматы**: 11 специалистов (55%)
- **Астана**: 3 специалиста (15%)
- **Шымкент**: 3 специалиста (15%)
- **Караганда**: 3 специалиста (15%)

### Общая статистика
- 📈 Средний рейтинг: **4.85/5.0**
- 💰 Диапазон цен: **1.2M - 12M KZT**
- 📅 Средний опыт: **21 год**
- ⭐ Средний reviewCount: **~190 отзывов**

---

## 🛠 Навигация в приложении

```
App.tsx
  └─ currentPage === 'production'
       └─ ProductionPage
            └─ ProductionShowcase
                 ├─ Статистика (4 карточки)
                 ├─ Актёры (5 карточек)
                 ├─ Режиссёры (5 карточек)
                 ├─ Режиссёры рекламы (5 карточек)
                 └─ Сценаристы (5 карточек)
```

---

## 🎨 UI элементы

### Баннер на главной
- Градиент: `from-purple-600 via-pink-600 to-violet-600`
- Бейдж "НОВОЕ"
- 2 кнопки действия
- Emoji 🎬

### Витрина
- 4 статистических карточки наверху
- Группировка по категориям
- Карточки артистов с hover-эффектами
- Призыв к действию внизу

### Каталог
- Фильтр по разделу "creative_production"
- Чекбоксы для ролей
- Сортировка, поиск, статистика

---

## 📝 Документация

Создано 3 новых документа:
1. **PRODUCTION_ARTISTS.md** - Детальная информация о каждом артисте
2. **PRODUCTION_QUICK_START.md** (этот файл) - Быстрый старт
3. **CHANGELOG.md** - Обновлён с новыми изменениями

---

## ⚡ Быстрые команды

### Посмотреть всех продакшн-артистов
```typescript
import { productionArtists } from './data/productionArtists';
console.table(productionArtists.map(a => ({
  name: a.name,
  role: a.roles[0],
  city: a.city,
  exp: a.experience,
  rating: a.rating
})));
```

### Экспорт в JSON
```typescript
import { productionArtists } from './data/productionArtists';
const json = JSON.stringify(productionArtists, null, 2);
console.log(json);
```

### Проверка типов
```typescript
import { Artist, ArtistRole } from './types/artist';
import { productionArtists } from './data/productionArtists';

// TypeScript автоматически проверяет типы
const validArtist: Artist = productionArtists[0]; ✅
```

---

## 🚦 Статус интеграции

| Компонент | Статус |
|-----------|--------|
| База данных (productionArtists.ts) | ✅ Готово |
| Типизация (types/artist.ts) | ✅ Готово |
| Интеграция (allKazakhArtists.ts) | ✅ Готово |
| Витрина (ProductionShowcase.tsx) | ✅ Готово |
| Страница (ProductionPage.tsx) | ✅ Готово |
| Навигация (Header.tsx) | ✅ Готово |
| Главная (HomePage.tsx) | ✅ Готово |
| Роутинг (App.tsx) | ✅ Готово |
| Документация | ✅ Готово |

---

## 🎉 Готово к использованию!

Все 20 продакшн-специалистов интегрированы и доступны:
- В каталоге с фильтрацией
- На отдельной витрине
- Через API/экспорты
- С полной типизацией

Запустите приложение и посетите:
- **Главная → Баннер "База продакшн-специалистов"**
- **Меню → 🎬 Продакшн**
- **Каталог → Раздел "Креатив и продакшн"**

---

*Создано: 14 января 2026*  
*Версия: 1.0*  
*Проект: TuranSound Platform*
