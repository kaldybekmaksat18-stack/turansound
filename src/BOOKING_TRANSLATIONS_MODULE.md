# Модуль переводов для системы бронирования

## Описание

Создан отдельный модуль `/lib/i18n/bookingTexts.ts` для переводов компонентов бронирования. Этот подход позволяет быстро добавлять переводы без необходимости редактирования огромного файла translations.ts.

## Использование

### 1. Импорт в компоненте

```typescript
import { useLanguage } from '../lib/i18n/LanguageContext';
import { bookingTexts } from '../lib/i18n/bookingTexts';

export function BookingModal() {
  const { language } = useLanguage();
  const bt = bookingTexts[language];
  
  return (
    <div>
      <h2>{bt.artistBooking}</h2>
      <p>{bt.step} 1 {bt.of} 3</p>
    </div>
  );
}
```

### 2. Альтернативный способ с хуком

```typescript
import { useLanguage } from '../lib/i18n/LanguageContext';
import { useBookingTexts } from '../lib/i18n/bookingTexts';

export function BookingModal() {
  const { language } = useLanguage();
  const bt = useBookingTexts(language);
  
  return <h2>{bt.artistBooking}</h2>;
}
```

## Охват переводов

Модуль `bookingTexts` содержит **все** переводы для:

### BookingModal
- ✅ Заголовки и шаги
- ✅ Поля форм (тип мероприятия, дата, место и т.д.)
- ✅ Типы мероприятий (свадьба, корпоратив, концерт и т.д.)
- ✅ Длительности (1 час, 2 часа, весь день и т.д.)
- ✅ Кнопки (Далее, Назад, Отправить заявку)
- ✅ Информационные блоки
- ✅ Сообщения успеха

### EnhancedBookingModal
- ✅ Расширенные заголовки ("Умное бронирование")
- ✅ Дополнительные услуги (звук, свет, видео, ведущий)
- ✅ AI-рекомендации
- ✅ Детализация стоимости
- ✅ Условия эскроу и смарт-контракта
- ✅ Все 5 шагов бронирования

### BookingsPage
- ✅ Заголовки и описания
- ✅ Статистика (всего, предстоящие, на эскроу, завершенные)
- ✅ Вкладки (Все, Предстоящие, Завершенные)
- ✅ Статусы бронирований
- ✅ Статусы платежей
- ✅ Действия (Контракт, Связаться, Отменить, Завершить, Отзыв)
- ✅ Диалог отмены бронирования
- ✅ Состояния загрузки и ошибок

## Структура данных

```typescript
interface BookingTexts {
  // Основные
  artistBooking: string;
  smartBooking: string;
  step: string;
  of: string;
  
  // Формы
  eventType: string;
  date: string;
  duration: string;
  location: string;
  city: string;
  guestCount: string;
  additionalNotes: string;
  yourName: string;
  phone: string;
  email: string;
  
  // Типы мероприятий
  wedding: string;
  corporate: string;
  birthday: string;
  festival: string;
  concert: string;
  restaurant: string;
  government: string;
  other: string;
  
  // Длительности
  hour1: string;
  hour2: string;
  hour3: string;
  hour4: string;
  hour6: string;
  hour8: string;
  fullDay: string;
  
  // Кнопки
  next: string;
  back: string;
  sendRequest: string;
  pay: string;
  close: string;
  
  // ... и многие другие ключи
}
```

## Поддерживаемые языки

- 🇰🇿 Казахский (kk) - 100%
- 🇷🇺 Русский (ru) - 100%
- 🇬🇧 Английский (en) - 100%

## Следующие шаги

1. Обновить компоненты BookingModal, EnhancedBookingModal и BookingsPage для использования этого модуля
2. Протестировать переключение языков во всех модалах бронирования
3. Добавить недостающие ключи, если будут обнаружены

## Преимущества подхода

✅ Не нужно редактировать огромный файл translations.ts  
✅ Все переводы бронирования в одном месте  
✅ Легко добавлять новые ключи  
✅ Типобезопасность через TypeScript  
✅ Автоматическое переключение при смене языка

---

**Дата создания**: 16 января 2026  
**Файл**: `/lib/i18n/bookingTexts.ts`  
**Статус**: Готов к использованию ✅
